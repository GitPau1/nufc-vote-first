# 툰 예산제 3단계 — 화면 표시(툰 비용·예산 바·선택불가) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 픽 화면의 배당(`×N.N`) 표기를 **툰 비용(Coins 아이콘 + 숫자)**으로 바꾸고, **5툰 예산 바**를 보여주며, **남은 예산으로 못 사는 선수를 선택 불가(흐림)**로 막는다. 결과·완료 화면의 배당 배지는 제거한다.

**Architecture:** 비용(`Candidate.cost`)은 이미 픽 화면까지 흐른다(2단계). 이 단계는 **표시 로직만** 바꾼다. 공용 배지·게이지는 `composition/predict/shared.tsx`에 `ToonCost`(lucide `Coins`)·`BudgetBar`(Radix Progress)로 추가한다. 예산 초과는 서버(2단계 `over_budget`)가 이미 막지만, 화면에서 **선택 단계에 미리** 막아 왕복을 없앤다.

**Tech Stack:** React/Next(클라이언트 컴포넌트), lucide-react(`Coins`, 기존 의존성), @radix-ui/react-progress(기존 의존성), Tailwind 디자인 토큰.

**전제:** 1·2단계 커밋 완료. 비용 데이터·`BUDGET=5`·`over_budget` 배관은 2단계에 있다.

**디자인 결정(확정):** 비용=Coins 아이콘+숫자 / 예산=가로 바 / 초과=초과 선수 선택 불가 / 결과·완료=배지 제거.

**설계 근거:** `docs/superpowers/specs/2026-08-30-toon-budget-prediction-design.md`

**범위 밖:** 월별 비용 산정 잡(4단계). 배당(`multiplier`) 컬럼/데이터 자체의 최종 제거는 이 단계 이후 별도 정리(이 단계는 화면 표시만 끊는다).

---

## 사전 확인 (근거)

- 픽 모달: `frontend/src/components/primitives/modal/contents/PlayerPick.tsx` — `PlayerPickCandidate`(`:11-23`, `multiplier` 보유), 배지(`:114` `×{multiplier}`), 행(`:80-116`).
- 픽 카드/흐름: `frontend/src/components/composition/predict/PredictionFlowClient.tsx` — pick 스텝(`:309-332`), 모달(`:410-429`), `PositionRow` 배지(`:612`), `PositionRow` 정의(`:584-630`), `over_budget` 메시지 이미 존재(`:46`).
- 공용: `frontend/src/components/composition/predict/shared.tsx`(`PlayerPhoto` 등). lucide-react 사용(package.json). Radix Progress는 `result-progress.tsx`가 사용 중.
- 결과: `frontend/src/components/composition/predict/PredictionResult.tsx:375-382`(모바일 `×배당`+점수). 데스크탑 카드엔 배당 없음.
- 완료: `frontend/src/components/composition/predict/PredictionDone.tsx:259`(모바일), `:287`(데스크탑) `×배당`.
- 스토리: `frontend/src/storybook/selection-and-input/PlayerPickModal.stories.tsx` — `multiplier` 사용(`:24,31-34,114,150`).
- 비용 데이터: `Candidate.cost`(`candidates.ts:24`), `BUDGET`(`submit.ts:17`), `POSITIONS`(`candidates.ts:8`).

---

## Task 1: 공용 컴포넌트 ToonCost + BudgetBar

**Files:**
- Modify: `frontend/src/components/composition/predict/shared.tsx`

- [ ] **Step 1: import 추가**

`frontend/src/components/composition/predict/shared.tsx:1-6`의 import 블록에 추가:

```tsx
'use client'

import { useState } from 'react'
import * as Progress from '@radix-ui/react-progress'
import { Coins } from 'lucide-react'
import { Avatar, AvatarFallback, AvatarImage } from '@/components/primitives/avatar'
import { Button } from '@/components/primitives/button'
import { badgeVariants } from '@/components/primitives/badge'
import { cn } from '@/lib/utils'
```

- [ ] **Step 2: ToonCost·BudgetBar 컴포넌트 추가**

`frontend/src/components/composition/predict/shared.tsx` 맨 끝(마지막 `}` 다음)에 추가:

```tsx

/**
 * 툰 비용 배지 — Coins 아이콘 + 숫자. 픽 카드·픽 모달에서 옛 배당(×N.N) 자리를 대체한다.
 * 색은 기존 Badge default(brand-weak/brand)를 그대로 쓴다.
 */
export function ToonCost({ cost, className }: { cost: number; className?: string }) {
  return (
    <span
      aria-label={`${cost}툰`}
      className={cn(badgeVariants(), 'inline-flex items-center gap-1', className)}
    >
      <Coins size={12} aria-hidden />
      {cost}
    </span>
  )
}

/**
 * 5툰 예산 사용량 바(가로 프로그레스). spent/total 비율로 채운다.
 * 초과는 선택 단계에서 막으므로(초과 선수 선택 불가) 여기서 over 상태는 그리지 않는다 — 최대 100%.
 */
export function BudgetBar({ spent, total = 5 }: { spent: number; total?: number }) {
  const percent = total > 0 ? Math.min(100, (spent / total) * 100) : 0
  return (
    <div className="flex items-center gap-2">
      <span className="shrink-0 text-caption-1 font-bold text-neutral-muted">예산</span>
      <Progress.Root
        value={percent}
        className="relative h-2 flex-1 overflow-hidden rounded-pill bg-disabled"
      >
        <Progress.Indicator
          className="h-full rounded-pill bg-brand-solid transition-transform duration-micro"
          style={{ transform: `translateX(-${100 - percent}%)` }}
        />
      </Progress.Root>
      <span className="shrink-0 text-caption-1 font-bold text-brand tabular-nums">
        {spent}/{total}툰
      </span>
    </div>
  )
}
```

- [ ] **Step 3: 타입 체크**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -E "shared.tsx|error" | head`
Expected: shared.tsx 관련 에러 없음(빈 출력). (`Coins`·`Progress`·`badgeVariants` 해석 확인.)

---

## Task 2: 픽 모달 — 배당→툰 + 선택 불가

**Files:**
- Modify: `frontend/src/components/primitives/modal/contents/PlayerPick.tsx`

- [ ] **Step 1: 인터페이스 multiplier→cost + remainingBudget prop**

`PlayerPick.tsx:11-23`의 `PlayerPickCandidate`에서 `multiplier`를 `cost`로 교체:

```tsx
export interface PlayerPickCandidate {
  /** season_squads.fotmob_player_id */
  id: number
  name: string
  /** 등번호 미정이면 null */
  squadNumber: number | null
  /** 없으면 실루엣 원형으로 대체된다 */
  photoUrl: string | null
  nationality: string | null
  age: number | null
  /** 툰 비용(1~3). 옛 배당을 대체한다. */
  cost: number
}
```

`PlayerPickContentProps`(`:25-36`)에 `remainingBudget` 추가(`onSelect` 위):

```tsx
interface PlayerPickContentProps {
  /** 모달 타이틀에 쓰일 라벨 — "미드필더 선택" */
  positionLabel: string
  players: PlayerPickCandidate[]
  /** 이미 이 포지션에 픽한 선수가 있으면(재오픈 케이스) 하이라이트 */
  selectedPlayerId?: number | null
  /**
   * 이 포지션에 쓸 수 있는 남은 툰(= 5 − 다른 두 포지션 비용 합). 비용이 이보다 큰 후보는
   * 선택 불가(흐림)로 막는다. 생략 시 제한 없음(Storybook 등).
   */
  remainingBudget?: number
  /**
   * 선수를 선택했을 때 호출된다. 선택과 동시에 닫지 않으므로,
   * 호출부에서 상태 반영 후 onOpenChange(false)까지 함께 호출해줘야 한다.
   */
  onSelect: (player: PlayerPickCandidate) => void
}
```

- [ ] **Step 2: import ToonCost**

`PlayerPick.tsx:6-9`의 import에 추가(badgeVariants 제거 — 이제 ToonCost가 대체):

```tsx
import { PlayerPhoto, ToonCost } from '@/components/composition/predict/shared'
import { cn } from '@/lib/utils'
import { SheetTitle } from '../sheet'
```

- [ ] **Step 3: Content가 remainingBudget을 행에 전달**

`PlayerPick.tsx:46-77`의 `PlayerPickContent`에서 `remainingBudget`을 받아 각 행에 `disabled`를 계산해 넘긴다. 함수 시그니처와 map을 교체:

```tsx
export function PlayerPickContent({
  positionLabel,
  players,
  selectedPlayerId,
  remainingBudget = Infinity,
  onSelect,
}: PlayerPickContentProps) {
  return (
    <>
      <SheetTitle className="mb-3 shrink-0 text-headline-2 font-extrabold text-neutral">{positionLabel} 선택</SheetTitle>

      <div className="min-h-0 flex-1 overflow-y-auto hide-scrollbar">
        {players.length === 0 ? (
          <p className="py-8 text-center text-caption-1 text-neutral-muted">
            선택할 수 있는 선수가 없어요
          </p>
        ) : (
          <div className="flex flex-col gap-2">
            {players.map(player => (
              <PlayerPickRow
                key={player.id}
                player={player}
                selected={player.id === selectedPlayerId}
                // 현재 선택된 선수는 예산을 넘겨도 다시 고를 수 있어야 한다(재오픈).
                disabled={player.cost > remainingBudget && player.id !== selectedPlayerId}
                onSelect={() => onSelect(player)}
              />
            ))}
          </div>
        )}
      </div>
    </>
  )
}
```

- [ ] **Step 4: 행 — 툰 배지 + 비활성 표시**

`PlayerPick.tsx:80-117`의 `PlayerPickRow`를 교체(배당 span → `ToonCost`, `disabled` 반영):

```tsx
function PlayerPickRow({
  player,
  selected,
  disabled,
  onSelect,
}: {
  player: PlayerPickCandidate
  selected: boolean
  disabled: boolean
  onSelect: () => void
}) {
  return (
    <button
      type="button"
      onClick={onSelect}
      disabled={disabled}
      aria-pressed={selected}
      className={cn(
        'flex w-full items-center justify-between gap-2.5 rounded-md border border-neutral-weak bg-surface px-3.5 py-2.5 text-left transition-[border-color,background-color,transform] duration-micro',
        selected
          ? 'border-brand-solid bg-brand-weak'
          : disabled
            ? 'cursor-not-allowed opacity-40'
            : 'hover:-translate-y-px hover:border-neutral-strong',
      )}
    >
      <span className="flex min-w-0 items-center gap-2.5">
        <span className="w-5 shrink-0 text-center text-label-2 font-extrabold text-neutral-muted">
          {player.squadNumber ?? '–'}
        </span>
        <PlayerPhoto url={player.photoUrl} />
        <span className="min-w-0">
          <p className="m-0 truncate text-body-2-normal font-bold text-neutral">{player.name}</p>
          <p className="m-0 mt-px text-caption-1 text-neutral-muted">
            {[player.nationality, player.age === null ? null : `${player.age}세`]
              .filter(Boolean)
              .join(' · ')}
          </p>
        </span>
      </span>
      <ToonCost cost={player.cost} className="shrink-0" />
    </button>
  )
}
```

---

## Task 3: 픽 카드(PositionRow) — 배당→툰

**Files:**
- Modify: `frontend/src/components/composition/predict/PredictionFlowClient.tsx:612`

- [ ] **Step 1: PositionRow 배지 교체**

`PredictionFlowClient.tsx:612`을 교체:

```tsx
                <ToonCost cost={picked.cost} />
```

(이 파일에 `ToonCost` import가 필요 — Task 4 Step 1에서 함께 추가한다.)

---

## Task 4: 예산 바 + 남은예산 배선 (PredictionFlowClient)

**Files:**
- Modify: `frontend/src/components/composition/predict/PredictionFlowClient.tsx`

- [ ] **Step 1: import 추가**

`PredictionFlowClient.tsx` 상단 import에 `ToonCost`·`BudgetBar`(shared), `BUDGET`(submit), `POSITIONS`(candidates)를 추가한다. 기존 import 라인을 찾아 보강:

- `@/components/composition/predict/shared`에서 가져오는 라인에 `ToonCost, BudgetBar` 추가(없으면 새 import 추가).
- `@/lib/predictions/submit`에서 `BUDGET`를 가져온다: `import { BUDGET } from '@/lib/predictions/submit'` (또는 기존 submit import에 병합).
- `@/lib/predictions/candidates`에서 `POSITIONS`를 가져온다(기존에 `POSITION_LABEL, type Position`을 가져오고 있으면 `POSITIONS` 추가).

- [ ] **Step 2: pick 스텝에 BudgetBar 삽입**

`PredictionFlowClient.tsx:326-330`의 `<PositionRow ... />`(pick 스텝) 바로 위에 예산 바를 넣고 간격을 준다. `</div>`(isMulti 블록 닫힘, `:326`) 다음, `<PositionRow`(`:327`) 앞에:

```tsx
                  <BudgetBar
                    spent={POSITIONS.reduce(
                      (sum, position) => sum + (picks[match.id]?.[position]?.cost ?? 0),
                      0,
                    )}
                  />
                  <div className="mt-2.5">
                    <PositionRow
                      picks={picks[match.id] ?? {}}
                      onOpen={position => setPickTarget({ matchId: match.id, position })}
                    />
                  </div>
```

(기존 `<PositionRow .../>`를 위 `<div className="mt-2.5">…</div>`로 감싼 형태로 교체한다.)

- [ ] **Step 3: 모달에 remainingBudget 전달**

`PredictionFlowClient.tsx:415-418`의 `<PlayerPickContent>`에 `remainingBudget` prop을 추가한다. `players=` 아래에:

```tsx
        <PlayerPickContent
          positionLabel={pickTarget ? POSITION_LABEL[pickTarget.position] : ''}
          players={pickTarget ? candidates[pickTarget.position] : []}
          remainingBudget={
            pickTarget
              ? BUDGET -
                POSITIONS.filter(position => position !== pickTarget.position).reduce(
                  (sum, position) => sum + (picks[pickTarget.matchId]?.[position]?.cost ?? 0),
                  0,
                )
              : BUDGET
          }
          selectedPlayerId={pickTarget ? picks[pickTarget.matchId]?.[pickTarget.position]?.id ?? null : null}
```

(나머지 `onSelect`는 그대로 둔다.)

---

## Task 5: 결과·완료 화면 배당 배지 제거

**Files:**
- Modify: `frontend/src/components/composition/predict/PredictionResult.tsx:375-382`
- Modify: `frontend/src/components/composition/predict/PredictionDone.tsx:259`, `:287`

- [ ] **Step 1: 결과 모바일 행에서 배당 span 제거**

`PredictionResult.tsx`의 픽 결과 행(`:375-382`)에서 `×배당` span만 제거하고 점수는 남긴다. 아래 블록을

```tsx
        {pick.multiplier !== null && (
          <span className="text-caption-2 font-bold text-neutral-muted">×{pick.multiplier.toFixed(1)}</span>
        )}
        <span className="text-body-2-normal font-black text-brand">{pick.points}점</span>
```

다음으로 교체(배당 span 삭제):

```tsx
        <span className="text-body-2-normal font-black text-brand">{pick.points}점</span>
```

- [ ] **Step 2: 완료 화면 배당 span 제거 (모바일·데스크탑)**

`PredictionDone.tsx:259`의 `<span ...>×{pick.multiplier.toFixed(1)}</span>` 한 줄을 삭제한다.
`PredictionDone.tsx:287`의 `<span ...>×{pick.multiplier.toFixed(1)}</span>` 한 줄을 삭제한다.

(각 행의 나머지 요소 — 포지션/이름/사진 — 는 그대로. `pick.multiplier` 필드는 데이터에 남지만 렌더만 끊는다. 미사용 필드는 lint 대상이 아님.)

- [ ] **Step 3: 타입/린트 확인**

Run: `cd frontend && npx tsc --noEmit 2>&1 | tail -20`
Expected: 에러 없음. (`multiplier` 참조가 모두 사라졌는지, `pick.multiplier`가 남은 곳이 없는지 확인.)

---

## Task 6: 스토리 동기화

**Files:**
- Modify: `frontend/src/storybook/selection-and-input/PlayerPickModal.stories.tsx`

- [ ] **Step 1: mock 후보의 multiplier→cost**

`PlayerPickModal.stories.tsx`에서 `multiplier`를 `cost`로 바꾼다:
- `:24` `multiplier: 1.7,` → `cost: 2,`
- `:31` `multiplier: 1.3` → `cost: 1`
- `:32` `multiplier: 1.5` → `cost: 2`
- `:33` `multiplier: 1.4` → `cost: 1`
- `:34` `multiplier: 1.9` → `cost: 3`
- `:114` `multiplier: 1 + (i % 9) / 10,` → `cost: (i % 3) + 1,`
- `:150` `(×${picked.multiplier.toFixed(1)})` → `(${picked.cost}툰)`

---

## Task 7: 전체 검증 + 커밋

- [ ] **Step 1: 테스트·타입·빌드**

Run: `cd frontend && npm test 2>&1 | grep -E "# (tests|pass|fail)" && npx tsc --noEmit && npm run build 2>&1 | tail -4`
Expected: 테스트 165 통과, tsc 에러 없음, 빌드 성공.

- [ ] **Step 2: 커밋**

```bash
git add frontend/src/components/composition/predict/shared.tsx \
        frontend/src/components/primitives/modal/contents/PlayerPick.tsx \
        frontend/src/components/composition/predict/PredictionFlowClient.tsx \
        frontend/src/components/composition/predict/PredictionResult.tsx \
        frontend/src/components/composition/predict/PredictionDone.tsx \
        frontend/src/storybook/selection-and-input/PlayerPickModal.stories.tsx
git commit -m "$(cat <<'EOF'
feat: 픽 화면 툰 비용 표시 + 5툰 예산 바 + 초과 선택 불가 (툰 3단계)

- 배당(×N.N) → 툰 비용 배지(ToonCost, Coins 아이콘+숫자): 픽 카드·모달
- BudgetBar(가로 프로그레스)로 경기별 5툰 사용량 표시
- 픽 모달: 남은 예산으로 못 사는 선수 선택 불가(흐림) — 서버 over_budget 전에 차단
- 결과·완료 화면의 배당 배지 제거(점수·평점만)
- Storybook PlayerPick 시안 cost로 동기화

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## 자체 검토 체크리스트 (작성자 확인 완료)

- **스펙/디자인 커버리지**: 툰 아이콘+숫자(ToonCost, Task 1·2·3) ✓, 가로 바(BudgetBar, Task 1·4) ✓, 초과 선택 불가(remainingBudget→disabled, Task 2·4) ✓, 결과·완료 배지 제거(Task 5) ✓.
- **플레이스홀더 스캔**: 없음. 모든 JSX/컴포넌트 완성형.
- **타입 정합**: `ToonCost`/`BudgetBar`(shared) → PlayerPick·PredictionFlowClient에서 import. `PlayerPickCandidate.cost`(모달) ← `Candidate.cost`(구조적 할당, Candidate가 cost 보유). `remainingBudget?` 옵셔널이라 스토리 무영향(단 multiplier→cost는 Task 6에서 동기화). `BUDGET`/`POSITIONS` import.
- **선택불가 안전장치**: 현재 선택 선수는 예산 초과여도 재선택 가능(`player.id !== selectedPlayerId`). remainingBudget은 "다른 두 포지션" 합으로 계산 → 재오픈 시 자기 비용 제외.
- **미제거 항목**: `multiplier` 데이터/컬럼은 남긴다(렌더만 끊음). 최종 제거는 이후 정리.
- **이 계획이 안 건드리는 것**: 점수 산식(1단계), 예산 검증 로직(2단계), 월별 잡(4단계).
