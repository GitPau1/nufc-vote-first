# 승부예측 결과 적재 · 정산 계획

작성일: 2026-08-24
대상 브랜치: `main` (HEAD `7bc5820`)
조사 방법: 코드 실측(grep/파일 확인) + `npx supabase functions list/download`로 배포된 Edge Function 5개 원본 확인
관련 문서: `20260823_프로젝트_전체점검.md`(§3), `10_gun/승부예측-랭킹-요구사항-명세서.md`, `00_의사결정사항/02-adr/002-fotmob-api-채택.md`

요구 3건을 구현하기 위한 계획이다.

1. 경기가 끝났을 때 한 번 데이터를 가져와 DB에 적재
2. 제출 완료 페이지에 맞았는지 틀렸는지 표기
3. 점수 집계는 주차가 다 끝나고 정산

---

## 0. 조사 결과 — 출발점이 생각과 다르다

`20260823_프로젝트_전체점검.md` §3.1은 "`fixtures`, `season_squads`, `fixture_player_ratings` 세 테이블에 쓰는 코드가 리포 전체에 없다"고 적었다. **틀린 게 아니라 리포 밖이었다.** 배포된 Edge Function 5개를 내려받아 확인한 결과:

| 함수(slug) | 하는 일 | DB write |
|---|---|---|
| `sync-fixture` | FotMob 팀 API → `fixtures` 전 경기 upsert. 멱등, POST 전용, dedupe·빈배열 방어까지 구현됨 | ✅ |
| `sync-season-squad` | `season_squads` 적재 (767줄) | ✅ |
| `match-ratings` | `matchDetails` → 뉴캐슬 선수 평점 **조회만** | ❌ |
| `get-fotmob-fixture` | 조회만 | ❌ |
| `health-check` | — | — |

추가로 확인한 사실:

- **앱 코드는 이 함수들을 한 번도 호출하지 않는다** (`grep -rn "functions/v1\|functions.invoke" src` → 0건). 전부 크론/수동 전용이다.
- **호출 트리거가 아예 없다.** pg_cron도, `vercel.json`도 없다. 즉 `sync-fixture`는 구현돼 있지만 아무도 부르지 않는다.
- `match-ratings`가 파싱(`extractLineupPlayers` / `extractRating`)을 이미 방어적으로 구현해뒀다. FotMob 응답 구조가 여러 갈래라 후보 경로를 순회하는 형태다.

**따라서 실제로 없는 것은 평점 write 하나 + 트리거다.** 채점·랭킹 view(`prediction_results`, `week_leaderboard`, `season_leaderboard`)와 결과 화면(`PredictionResult`, `getMyResults`, `getWeekRanking`)은 §3.2 지적 이후 이미 연결됐다.

---

## 1. 0단계 — Edge Function 리포 편입 — ✅ 완료 (2026-08-24)

`supabase/functions/` 5개를 배포본 그대로 커밋 (`dc174a6` "기존에 작성된 edge function 백업", PR #11). 배포본과 동일한 기준점이 생겼고 이후 변경은 diff로 남는다. `config.toml`은 손대지 않았다(기본값으로 배포된다).

마이그레이션은 리포에서 관리되는데 함수만 대시보드에 있으면, §2("가이드가 없는 파일을 가리킨다")와 같은 종류의 드리프트가 또 쌓인다. 그 구멍이 닫혔다.

---

## 2. 1단계 — 평점 적재: `match-ratings` → `sync-fixture-ratings` — ✅ 완료 (2026-08-24)

새 함수를 만들지 않고 기존 조회 함수를 개명·전환한다. 파싱이 이미 있고, 앱에서 호출하는 곳이 0건이라 조회 응답을 유지할 이유가 없다.

```
supabase/functions/sync-fixture-ratings/index.ts   (match-ratings 이동 + 껍데기 교체)
```

### 대상 선별 = 멱등성

`fixtures`에서 `finished = true`인 경기 중, 적재된 `fixture_player_ratings` 행이 **11개 미만**인 것. 임베디드 count(`select=fixture_id,fixture_player_ratings(count)`)로 한 번에 뽑는다 — FK가 있어 그대로 동작한다.

"행이 하나라도 있으면 적재 완료"로 판정하지 않는 이유:

> FotMob 평점은 경기 종료 시점에 확정되지 않는다. 종료 직후에는 일부 선수만 채워져 내려올 수 있다. 그 상태로 적재하고 "행이 있으니 완료"로 굳으면, 나머지 선수는 `prediction_pick_points`에서 **영구히 0점**이 된다. 조용히 틀린 점수가 남는 형태라 가장 위험하다.

선발 11명을 기준선으로 두면 부분 적재가 다음 실행에서 자동으로 재시도되고, 연장·퇴장 같은 예외에도 깨지지 않는다.

### 배치 상한

`limit(5)` + 응답에 남은 경기 수. 두 가지를 동시에 막는다.

- **CPU 2초 제한**: 무료 플랜 Edge Function은 호출당 wall clock 150초, CPU 2초다. `matchDetails` 응답이 경기당 수백 KB~1MB급이라 파싱만 경기당 수십 ms. 첫 백필에서 시즌 40경기를 한 번에 돌면 CPU 1.5~2초로 제한에 걸려 중간에 죽고, 앞쪽만 적재된 채 성공한 것처럼 보인다.
- FotMob rate limit / 구조 변경 시 한 번에 40번 때리는 것.

백필은 며칠에 걸쳐 알아서 끝나거나 관리자 버튼을 몇 번 누르면 끝난다.

### 쓰기

`rating !== null`인 선수만 `fixture_player_ratings` upsert(`onConflict: 'fixture_id,player_id'`). 평점 없는 선수는 행을 만들지 않고, 그게 그대로 0점이다 — FR-015(선수 미출전 0점)가 그대로 유지된다.

껍데기는 `sync-fixture`와 동일하게 맞춘다(POST 전용, service-role, 요약 JSON 반환). 디버깅용 `?matchId=` 파라미터만 남긴다.

### 배포

```bash
npx supabase functions deploy sync-fixture-ratings
npx supabase functions delete match-ratings
```

### 실행 결과 (2026-08-24)

배포 후 수동 실행 2회로 백필 완료. 3회차는 `processed=0` — 멱등성 확인.

| 경기 | 킥오프(KST) | 스코어 | 적재 평점 행 |
|---|---|---|---|
| 5795371 | 08/24 00:30 | 2-2 | 15 |
| 5918243 | 08/17 00:00 | 1-1 | 14 |
| 5874663 | 08/15 23:00 | 1-2 | 13 |
| 5739316 | 08/13 01:15 | 3-1 | 21 |
| 5898826 | 08/09 04:00 | 1-2 | 20 |
| 5898825 | 07/30 03:30 | 4-1 | 16 |
| 5898824 | 07/25 20:30 | 1-1 | 17 |
| 4813748 | 05/25 00:00 | 2-0 | 15 |

종료된 8경기 전부, 131행. 평점 분포도 정상(6.0~7.8). 행 수가 13~21로 갈리는 건 프리시즌
친선경기에서 교체가 많았기 때문이고, `MIN_RATED_PLAYERS = 11` 기준선 아래로 내려간 경기는 없다.

`match-ratings`는 백필 확인 후 삭제했다. 배포된 함수는 `health-check`, `get-fotmob-fixture`,
`sync-fixture`, `sync-season-squad`, `sync-fixture-ratings` 5개다.

---

## 3. 2단계 — 크론 하루 1회 (KST 08:00) — ✅ 완료 (2026-08-24)

### 왜 하루 1회인가

1. **경기 시간대가 이미 하루 1회로 수렴한다.** EPL 킥오프를 KST로 옮기면 토 23:00, 일 00:30~02:00, 늦은 경기 04:00~05:00. 컵 미드위크도 KST 04:00~05:00. **전 경기가 KST 07시 전에 끝난다.** 08:00 실행이면 어제 끝난 경기가 한 번에 다 잡힌다. 매시간 돌리면 24회 중 23회가 no-op이다.
2. **지연 1시간과 8시간의 차이가 사용자에게 없다.** 새벽에 끝난 경기의 결과를 보러 오는 사람은 아침에 온다.
3. **촘촘한 폴링이 오히려 위험하다.** 종료 직후를 때리면 §2의 "평점 미확정 창"에 정확히 들어간다. 종료 후 몇 시간 지난 뒤 한 번 읽는 게 데이터 품질 면에서 낫다.

### 부담

| 항목 | 사용량 | 무료 한도 | 비중 |
|---|---|---|---|
| Edge Function 호출 | 2회/일 ≈ 60회/월 | 500,000회/월 | 0.01% |
| FotMob 호출 | 팀 API 1회 + 경기당 1회 = 하루 1~3회 | — | — |
| DB 쓰기 | 경기당 약 18행 → 시즌 전체 약 900행 | — | 무의미 |
| egress | 요약 JSON 몇 KB (FotMob 수신은 인바운드라 미집계) | 5GB/월 | ~0% |

여유가 4자리수 배다. 실제로 신경 쓸 건 주기가 아니라 배치 상한 하나뿐이다.

### 등록 방법 — 대시보드 (등록 완료, 실행 확인됨)

**Supabase 대시보드 → Integrations → Cron**에서 job 2개를 만든다. 두 함수가 `verify_jwt: true`라 호출에 service_role 키가 필요한데, 마이그레이션에 넣으면 키가 커밋된다. Vault 참조로 우회할 수도 있지만 등록은 한 번이라 그만큼의 SQL을 살 이유가 없다.

| job | 스케줄(UTC) | KST | 대상 | Timeout |
|---|---|---|---|---|
| `sync-fixture-daily` | `0 23 * * *` | 08:00 | Edge Function `sync-fixture` (POST) | 10000ms |
| `sync-fixture-ratings-daily` | `5 23 * * *` | 08:05 | Edge Function `sync-fixture-ratings` (POST) | 30000ms |

pg_cron job 하나에서 두 함수를 순차 호출하려면 SQL을 써야 하므로, **5분 간격 job 2개로 나눴다.** 스코어가 먼저 들어와야 평점 대상이 잡히는 순서 의존성이 이 간격으로 해결된다. 실패해도 다음 날 자동 재시도되고, 급하면 관리자 버튼을 누르면 된다.

**타임존**: pg_cron은 DB 타임존(Supabase 기본 GMT/UTC) 기준으로 스케줄을 해석하고, Cron UI 문구("At 23:00")는 cron 식을 그대로 읽어 보여준다. 즉 UTC 23:00 = KST 08:00이 맞고, UI가 브라우저 시간대로 변환하는 것이 아니다.

**Method는 POST 필수** — 두 함수 모두 POST 전용이라 GET이면 405다. **Timeout 기본값(1000ms)은 너무 짧다** — FotMob 왕복이 들어가고, 평점 쪽은 한 번에 최대 5경기를 돌기 때문에 여유를 준다.

**Authorization 헤더는 UI가 채워주는 anon 키 그대로 둔다.** `verify_jwt: true`는 프로젝트가 서명한 유효한 JWT인지만 확인하고 anon 키가 그 조건을 만족한다. DB 쓰기는 함수가 자기 환경변수의 service-role 키로 하므로 크론 쪽에 비밀키를 둘 일이 없다 — 계획 단계에서 "키가 커밋된다"를 걱정했지만, 애초에 크론에 service-role 키가 필요하지 않았다.

기록은 `supabase/functions/README.md`.

### 관리자 수동 트리거 — ✅ 완료 (2026-08-24)

```
frontend/src/lib/actions/sync-fixtures.ts        (신규) 두 함수 순차 POST + 결과 합산
frontend/src/components/admin/AdminSyncButton.tsx (신규) 버튼 + 결과/에러 메시지
frontend/src/app/admin/page.tsx                   버튼 배치
frontend/src/lib/queries/fixtures.ts              캐시 태그 추가
```

`/admin`의 "경기 결과·평점 동기화" 버튼. 백필, 경기 연기, FotMob 평점 지연·구조 변경 후 재적재 — 예외는 사람이 즉시 처리하는 편이 폴링 주기를 촘촘하게 만드는 것보다 유용하다. 배치 상한 때문에 남은 경기가 있으면 그 수를 메시지로 알려준다. 기존 `/admin/ratings`(수동 평점 입력)는 보정용으로 그대로 둔다.

**계획에서 벗어난 것 1건**: §8에서 "건너뜀"으로 분류했던 Next 캐시 즉시 무효화를 넣었다(`getFixtureWeeks`에 `tags: ['fixture-weeks']` + 액션에서 `revalidateTag`). 두 줄이고, 이게 없으면 버튼을 눌러도 최대 5분간 화면이 그대로여서 동기화가 실패한 것처럼 보인다 — 버튼의 존재 이유가 "즉시"라 같이 넣는 게 맞다.

---

## 4. 3단계 — 제출 완료 화면 O/X 표기 — ✅ 완료 (2026-08-24)

DB·쿼리 변경 0건. `MatchView`가 이미 `finished`와 `actual`([우리, 상대])을 들고 `PredictionDone`까지 내려간다.

- `lib/predictions/result.ts`에 순수 함수 추가: `matchHit(predicted, actual) → 'exact' | 'outcome' | 'miss'`. SQL `prediction_match_points`와 같은 기준이므로 양쪽에 상호 참조 주석을 남긴다. `result.test.mjs`에 케이스 3개.
- `PredictionDone`의 경기 카드: 종료된 경기면 예측 스코어 아래에 실제 스코어 + 배지(`정확히 적중` / `승패 적중` / `미적중`).
- **점수 숫자는 표시하지 않는다** — 4단계(정산 게이트)와 일관되게. 선수 픽 O/X도 제외한다(평점이 곧 점수라 정산 화면 몫).

```
frontend/src/lib/predictions/result.ts          matchHit() 추가
frontend/src/lib/predictions/result.test.mjs    3단계 판정 케이스 5개
frontend/src/components/predict/PredictionDone.tsx  실제 결과 블록 + HitBadge
frontend/src/app/predictions/[weekKey]/page.tsx     notFound 조건 축소
```

배지 색은 결과 화면 `PointsBadge`와 같은 체계(`bg-positive-dim` / `bg-negative-dim`)를 쓰되 점수 대신 등급만 말한다. 스코어가 아직 안 들어온 경기는 결과 화면과 같은 문구("스코어 집계 중")로 둔다.

**덧붙인 수정 1건**: 그 주 경기가 전부 킥오프을 지났으면 카운트다운을 그리지 않는다. 페이지 조건을 넓힌 뒤로는 "모든 경기가 킥오프을 지났지만 아직 종료 적재 전"인 주차도 이 화면으로 들어오는데, 그때 카운트다운이 `0일 00:00:00`에 붙어 있게 된다.

`tsc --noEmit` 클린, `npm test` 132/132, `next build` 성공.

### `[weekKey]/page.tsx` 조건 수정 (필수)

현재는 `week.status === 'upcoming'`이면 `notFound()`다. 그런데 킥오프이 지나고 아직 `finished`가 적재되지 않은 구간은 "모든 경기 locked + 전부 finished 아님" → `upcoming`으로 판정된다(`week.ts`의 `weekStatus`). **하루 1회 크론이면 경기가 끝난 새벽부터 08:00까지 페이지가 사라진다.**

아직 열리지 않은 주차만 막도록 좁힌다.

```ts
if (!week || (week.status === 'upcoming' && week.matches.every(m => !m.locked))) notFound()
```

- 아직 오픈 전 주차: 모든 경기가 `locked = false` → `notFound()` 유지.
- 경기 진행 중 / 종료 후 적재 전: 모두 `locked = true` → 통과해서 완료 화면(`pending`이 비어 `PredictionDone`)으로 떨어지고, 적재되면 같은 화면에 O/X가 붙는다.
- 경기 없는 빈 주차: `every`가 true → `notFound()` 유지.

---

## 5. 4단계 — 주차 정산 게이트 — ✅ 완료 (2026-08-24)

`prediction_results` view를 `create or replace`(컬럼 변화 없음)하고 "그 주차에 안 끝난 경기가 없다" 조건을 추가한다.

```sql
and not exists (
  select 1 from public.fixtures f2
  where f2.cancelled = false and f2.finished = false and f2.kickoff_at is not null
    and date_trunc('week', f2.kickoff_at at time zone 'Asia/Seoul')
      = date_trunc('week', f.kickoff_at  at time zone 'Asia/Seoul')
)
```

- `week_leaderboard` / `season_leaderboard`가 이 view 위에 있어 자동 적용된다. 더블 매치위크에서 첫 경기 점수가 시즌 랭킹으로 새는 것을 막는다.
- 킥오프 미정(`kickoff_at is null`)과 취소 경기는 세지 않는다 — `groupFixturesByWeek`과 같은 기준이라 화면/DB 판정이 어긋나지 않는다.
- 화면 정합은 이미 맞다: `status === 'result'`(주차 전 경기 종료)와 settled가 항상 동시에 성립하므로, 결과 화면이 빈 점수로 뜨는 구간이 없다.

### `weekly_scores` 테이블은 만들지 않는다

점수는 배당 스냅샷(`predictions.*_multiplier`) + 평점(`fixture_player_ratings`)으로 완전히 파생되고, "산식을 고치면 과거 경기 점수까지 같이 따라온다"가 기존 설계의 명시적 의도다(`20260821120000_create_predictions.sql` 주석). 지금 필요한 건 저장이 아니라 **노출 게이트**다.

정산 시점을 크론 스케줄로 통제하는 방안도 검토했으나 기각했다. 취소·연기 경기(FotMob이 `cancelled`를 내려주고 킥오프이 다음 주로 밀리는 경우)에서 바로 깨진다. 데이터 상태로 판정하면 그게 알아서 맞는다. **"언제 가져오나"(크론)와 "언제 공개하나"(view 조건)를 분리하는 것이 요구 3건을 동시에 만족시키는 가장 짧은 길이다.**

랭킹 조회가 무거워지면 그때 matview + 갱신으로 승격한다.

### 적용 결과 (2026-08-24)

`supabase/migrations/20260824120000_prediction_results_week_settled.sql` → `supabase db push` 완료.

적용 직후 `prediction_results` / `week_leaderboard` / `season_leaderboard` 세 view가 **3행 → 0행**이 됐다. 이번 주차(2026-35)에 리버풀전(8/24, 종료)과 웨스트브롬(8/27)·토트넘(8/30) 두 경기가 같이 들어 있어서, 종료된 한 경기의 점수가 주차 진행 중에 랭킹으로 새던 것이 정확히 막혔다.

게이트가 "항상 막는" 게 아닌 것도 확인했다 — 종료된 8경기에 대해 정산 판정을 직접 조회한 결과:

| 경기 | 주차 | settled |
|---|---|---|
| 5795371 | 2026-35 | **false** (미종료 2경기 남음) |
| 5918243 | 2026-34 | true |
| 5874663 / 5739316 | 2026-33 | true |
| 5898826 | 2026-32 | true |
| 5898825 | 2026-31 | true |
| 5898824 | 2026-30 | true |
| 4813748 | 2026-22 | true |

SQL이 계산한 `week_key`(`IYYY-IW`)도 `lib/predictions/week.ts`의 `weekKey()` 결과와 일치한다. 2026-35 주차가 끝나면(8/30 토트넘전 종료 + 평점 적재) 그 주차 점수와 랭킹이 한꺼번에 공개된다.

---

## 6. 5단계 — 문서 갱신 — ✅ 완료 (2026-08-24)

- `99_old/SUPABASE_DATA_CONNECTIONS.md`: "Edge Function · 크론" 절 신설(함수 4종 + 크론 2건 + 배치/타임존/인증 근거), `fixtures` RLS 설명에 적재 주체 명시, 승부예측 절에 정산 게이트·`matchHit`·404 조건·평점 쓰기 경로 2개 반영, 위험 지점에 FotMob 비공식 API 항목 추가. 최종 업데이트 날짜 갱신.
- `99_old/AGENT_MAINTENANCE_GUIDE.md`: "조심할 부분"에 함수 소스 관리 위치(대시보드에서 직접 고치지 말 것)와 정산 게이트 추가, 승부예측 기능별 파일에 자동 적재·수동 트리거·`matchHit`·404 조건 추가, "아직 없는 것"에서 평점 자동 주입 제거. 최종 업데이트 날짜 갱신.
- `20260823_프로젝트_전체점검.md`: §3 헤더에 정정 블록 추가("write 경로 0건"은 리포 안에서만 맞았음 — 실제로 없던 것은 평점 write + 트리거), §3.3의 FR-008·FR-017 행과 요약 표 갱신. 점검 당시 기록은 그대로 남겼다.
- `10_gun/승부예측-랭킹-요구사항-명세서.md`: **손대지 않았다.** 그 문서의 "상태" 컬럼은 요구사항 자체의 확정 상태(초안/확정)이고 구현 상태 컬럼이 없어서, 구현 진척을 적을 자리가 아니다. 게다가 작업 시점에 커밋되지 않은 사용자 수정이 있었다. 구현 대비 명세 현황은 전체점검 §3.3이 담당한다.

---

## 7. 작업 순서와 규모

| 단계 | 내용 | 규모 |
|---|---|---|
| 0 | Edge Function 5개 리포 편입 | ✅ 완료 (`dc174a6`) |
| 1 | `sync-fixture-ratings` 전환 + 배포 + 백필 | ✅ 완료 (8경기 131행) |
| 2 | 관리자 버튼 | ✅ 완료 |
| 2b | pg_cron 등록 | ✅ 완료 (job 2개, 실행 확인) |
| 3 | `matchHit` + `PredictionDone` 배지 + 페이지 조건 | ✅ 완료 (132/132) |
| 4 | 정산 게이트 마이그레이션 | ✅ 완료 (3행 → 0행 확인) |
| 5 | 문서 갱신 | ✅ 완료 (문서 3개) |

**5단계까지 전부 완료.** 요구 3건이 모두 동작한다. 1단계를 먼저 끝낸 이유는 실데이터 없이 3·4단계를 검증할 수 없기 때문이다.

## 8. 확정 사항과 건너뛴 것

**확정**: `match-ratings` 조회 응답 폐기, 관리자 수동 버튼 포함, 크론 하루 1회. 함수 편입은 별도 커밋으로 분리해 완료(`dc174a6`).

**건너뛴 것**

| 항목 | 추가할 시점 |
|---|---|
| ~~적재 후 Next 캐시 즉시 무효화~~ | 2단계에서 추가함 (`tags: ['fixture-weeks']` + `revalidateTag`) — 관리자 버튼의 존재 이유가 "즉시"라서 |
| 선수 픽 O/X 표기 | 평점 = 곧 점수. 정산 전 노출 정책이 바뀌면 |
| `weekly_scores` 테이블 / matview | 랭킹 조회가 실제로 느려지면 |
| 시간대별 촘촘한 폴링 | 하루 1회 지연이 실제로 문제가 되면 |
| `fixture_player_ratings.source` 컬럼 | 자동/수동 입력을 구분해 추적할 필요가 생기면 |

## Related

- `20260823_프로젝트_전체점검.md`
- `10_gun/승부예측-랭킹-요구사항-명세서.md`
- `00_의사결정사항/02-adr/002-fotmob-api-채택.md`
