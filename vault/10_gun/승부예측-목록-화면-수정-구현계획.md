# 승부예측 목록 화면 수정 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/predictions` 목록 화면을 시안대로 고친다 — 활성 주차 최상단 정렬, 경기 카드 균등 분배 가로 배치, 플레이 방법 아코디언, 주차 상태 5단계 시각 구분.

**Architecture:** 주차 컨테이너를 `<button>` → `<div>`로 바꾸고 진입점을 하단 CTA 버튼 하나로 옮긴다(`MatchdayHero` 패턴). 주차 상태는 `weekStatus`를 건드리지 않고 표시 레이어에서 경기별 `locked`/`finished`로 5단계를 파생한다. 플레이 방법은 범용 `Explanation` 컴포넌트 + 신규 `accordion` 프리미티브로 만든다.

**Tech Stack:** Next.js 14 App Router, Tailwind(semantic 토큰만), Radix UI, node:test(`*.test.mjs`), Storybook.

**근거 문서:** `vault/10_gun/승부예측-목록-화면-수정-시안.md`

---

## 실행 순서 (독립 작업 병렬)

| Wave | 태스크 | 병렬 여부 | 의존 |
| --- | --- | --- | --- |
| 1 | T1 `week.ts` · T2 `accordion` · T3 `TeamBadge` | **3개 동시** | 없음 (서로 다른 파일) |
| 2 | T4 `MatchWeekList` · T5 `Explanation` | **2개 동시** | T4←T3, T5←T2 |
| 3 | T6 `MatchWeekList.stories` · T7 `PredictListClient` | **2개 동시** | T6←T4, T7←T1·T5 |
| 4 | T8 전체 검증 | 단독 | 전부 |

같은 Wave 안의 태스크는 파일이 겹치지 않으므로 동시에 진행한다. Wave가 끝날 때마다 `npm test`가 통과하는지 확인하고 넘어간다.

---

## 파일 구조

| 파일 | 책임 |
| --- | --- |
| `frontend/src/lib/predictions/week.ts` | 순수 변환 — 일자 포맷, 목록 정렬 함수 추가 |
| `frontend/src/components/primitives/accordion.tsx` | **신규** — 펼침/접힘 프리미티브 (Radix) |
| `frontend/src/components/composition/common/Explanation.tsx` | **신규** — 제목 + 항목 리스트를 접힌 아코디언으로 보여주는 범용 설명 블록 |
| `frontend/src/components/composition/predict/shared.tsx` | `TeamBadge`에 `grayscale` prop |
| `frontend/src/components/composition/predict/MatchWeekList.tsx` | 주차 컨테이너 · 경기 카드 · 상태 파생 · CTA |
| `frontend/src/components/composition/predict/PredictListClient.tsx` | 정렬 적용 · 2단 배치 · 플레이 방법 배치 · 트래킹 |

---

### Task 1: `week.ts` — 일자 포맷과 목록 정렬

**Files:**
- Modify: `frontend/src/lib/predictions/week.ts:145-167` (`toMatchView`), 파일 끝에 함수 추가
- Test: `frontend/src/lib/predictions/week.test.mjs:168` 수정 + 새 테스트 추가

- [ ] **Step 1: 일자 포맷 단정문을 새 형식으로 바꾼다 (실패하는 테스트)**

`frontend/src/lib/predictions/week.test.mjs`의 168번째 줄을 바꾼다.

```js
  // 한국시간(UTC+9) 기준 표기
  assert.equal(view.kickoff, '8월 23일')
```

- [ ] **Step 2: 실패 확인**

```bash
cd frontend && node --test src/lib/predictions/week.test.mjs
```
Expected: FAIL — `'8/23' !== '8월 23일'`

- [ ] **Step 3: 포맷터를 고친다**

`frontend/src/lib/predictions/week.ts:157`을 바꾼다.

```ts
    kickoff: kst ? `${kst.getUTCMonth() + 1}월 ${kst.getUTCDate()}일` : '',
```

같은 파일 48번째 줄의 주석도 함께 고친다.

```ts
  /** '8월 23일' */
  kickoff: string
```

- [ ] **Step 4: 통과 확인**

```bash
cd frontend && node --test src/lib/predictions/week.test.mjs
```
Expected: PASS

- [ ] **Step 5: 정렬 함수의 실패하는 테스트를 쓴다**

`frontend/src/lib/predictions/week.test.mjs` 맨 아래에 추가한다. 파일 상단에서 이미 `loadWeekModule()`로 모듈을 읽고 있으니 같은 방식으로 함수를 꺼낸다(기존 테스트들이 쓰는 구조를 그대로 따른다).

```js
test('openWeeksFirst: 활성 주차만 앞으로 올리고 나머지 순서는 그대로 둔다', () => {
  const { openWeeksFirst } = loadWeekModule()

  const weeks = [
    { weekKey: '2026-33', status: 'result' },
    { weekKey: '2026-34', status: 'result' },
    { weekKey: '2026-35', status: 'open' },
    { weekKey: '2026-36', status: 'upcoming' },
  ]

  assert.deepEqual(
    openWeeksFirst(weeks).map(week => week.weekKey),
    ['2026-35', '2026-33', '2026-34', '2026-36'],
  )
})

test('openWeeksFirst: 활성 주차가 없으면 순서가 안 바뀐다', () => {
  const { openWeeksFirst } = loadWeekModule()

  const weeks = [
    { weekKey: '2026-33', status: 'result' },
    { weekKey: '2026-34', status: 'upcoming' },
  ]

  assert.deepEqual(
    openWeeksFirst(weeks).map(week => week.weekKey),
    ['2026-33', '2026-34'],
  )
})

test('openWeeksFirst: 활성 주차가 둘이면 둘 다 앞으로 오고 서로의 순서는 유지된다', () => {
  const { openWeeksFirst } = loadWeekModule()

  const weeks = [
    { weekKey: '2026-33', status: 'result' },
    { weekKey: '2026-34', status: 'open' },
    { weekKey: '2026-35', status: 'open' },
  ]

  assert.deepEqual(
    openWeeksFirst(weeks).map(week => week.weekKey),
    ['2026-34', '2026-35', '2026-33'],
  )
})
```

- [ ] **Step 6: 실패 확인**

```bash
cd frontend && node --test src/lib/predictions/week.test.mjs
```
Expected: FAIL — `openWeeksFirst is not a function`

- [ ] **Step 7: 정렬 함수를 구현한다**

`frontend/src/lib/predictions/week.ts`의 `toPredictWeeks` **바로 앞**에 추가한다. (같은 파일의 목록 화면용 변환들과 같은 자리다.)

```ts
/**
 * 목록 화면 정렬 — 예측 접수 중인(`open`) 주차를 맨 위로 올리고, 나머지는 받은 순서(킥오프
 * 오름차순)를 그대로 유지한다. 월 필터는 호출부가 이미 적용한 뒤 넘긴다.
 *
 * 상단에 와야 하는 것은 "그 달의 첫 주차"가 아니라 "지금 예측할 수 있는 주차"다 —
 * 오름차순만 쓰면 이번 주차가 지난 주차들 아래에 묻힌다(2026-08-25 확정).
 */
export function openWeeksFirst(weeks: WeekGroup[]): WeekGroup[] {
  return [
    ...weeks.filter(week => week.status === 'open'),
    ...weeks.filter(week => week.status !== 'open'),
  ]
}
```

- [ ] **Step 8: 통과 확인**

```bash
cd frontend && node --test src/lib/predictions/week.test.mjs
```
Expected: PASS (기존 테스트 포함 전부)

- [ ] **Step 9: 커밋**

```bash
git add frontend/src/lib/predictions/week.ts frontend/src/lib/predictions/week.test.mjs
git commit -m "승부예측 목록 일자 표기를 '8월 23일' 형식으로 바꾸고 활성 주차 우선 정렬 함수 추가"
```

---

### Task 2: `accordion` 프리미티브 신설

**Files:**
- Create: `frontend/src/components/primitives/accordion.tsx`
- Create: `frontend/src/storybook/contents/Accordion.stories.tsx`
- Modify: `frontend/package.json` (의존성)
- Modify: `frontend/src/components/design-foundation.test.mjs` (검사 목록)

- [ ] **Step 1: Radix accordion 설치**

```bash
cd frontend && npm install @radix-ui/react-accordion
```
Expected: `added 1 package` 류의 출력. `package.json`의 `dependencies`에 `"@radix-ui/react-accordion"`이 생긴다.

- [ ] **Step 2: 검사 목록에 새 파일을 먼저 등록한다 (실패하는 테스트)**

`frontend/src/components/design-foundation.test.mjs`의 `'application source does not use arbitrary typography or hardcoded visual colors'` 테스트 안 `files` 배열에서, `'components/primitives/modal/contents/PollPicker.tsx'` 줄 **바로 아래**에 추가한다.

```js
    'components/primitives/accordion.tsx',
```

- [ ] **Step 3: 실패 확인**

```bash
cd frontend && node --test src/components/design-foundation.test.mjs
```
Expected: FAIL — `ENOENT: no such file or directory ... accordion.tsx`

- [ ] **Step 4: 프리미티브를 만든다**

`frontend/src/components/primitives/accordion.tsx` 신규 작성. `separator.tsx`와 같은 forwardRef + `cn` 구조를 따르고, 색·타이포는 semantic 토큰만 쓴다(임의값 금지 검사 대상이다).

```tsx
"use client"

import * as React from "react"
import * as AccordionPrimitive from "@radix-ui/react-accordion"
import { ChevronDown } from "lucide-react"

import { cn } from "@/lib/utils"

/**
 * 펼침/접힘 블록. 기본은 접힌 상태다 — Radix의 `defaultValue`를 안 넘기면 아무것도 안 펼쳐진다.
 * 열림 애니메이션은 넣지 않았다: 높이 트랜지션은 `--radix-accordion-content-height`를 쓰는
 * 커스텀 keyframe이 필요한데, 그러려면 임의값 클래스가 들어가서 foundation 검사와 부딪힌다.
 */
const Accordion = AccordionPrimitive.Root

const AccordionItem = React.forwardRef<
  React.ElementRef<typeof AccordionPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof AccordionPrimitive.Item>
>(({ className, ...props }, ref) => (
  <AccordionPrimitive.Item
    ref={ref}
    className={cn("rounded-lg border border-neutral-weak bg-surface", className)}
    {...props}
  />
))
AccordionItem.displayName = "AccordionItem"

const AccordionTrigger = React.forwardRef<
  React.ElementRef<typeof AccordionPrimitive.Trigger>,
  React.ComponentPropsWithoutRef<typeof AccordionPrimitive.Trigger>
>(({ className, children, ...props }, ref) => (
  <AccordionPrimitive.Header className="flex">
    <AccordionPrimitive.Trigger
      ref={ref}
      className={cn(
        "flex flex-1 items-center justify-between gap-2 p-4 text-left text-label-1-normal font-bold text-neutral transition-opacity duration-micro hover:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-solid [&[data-state=open]>svg]:rotate-180",
        className
      )}
      {...props}
    >
      {children}
      <ChevronDown className="h-4 w-4 shrink-0 text-neutral-muted transition-transform duration-micro" />
    </AccordionPrimitive.Trigger>
  </AccordionPrimitive.Header>
))
AccordionTrigger.displayName = "AccordionTrigger"

const AccordionContent = React.forwardRef<
  React.ElementRef<typeof AccordionPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof AccordionPrimitive.Content>
>(({ className, children, ...props }, ref) => (
  <AccordionPrimitive.Content
    ref={ref}
    className={cn("overflow-hidden px-4 pb-4 text-caption-1 text-neutral-muted", className)}
    {...props}
  >
    {children}
  </AccordionPrimitive.Content>
))
AccordionContent.displayName = "AccordionContent"

export { Accordion, AccordionItem, AccordionTrigger, AccordionContent }
```

- [ ] **Step 5: 통과 확인**

```bash
cd frontend && node --test src/components/design-foundation.test.mjs
```
Expected: PASS

- [ ] **Step 6: 스토리를 쓴다**

`frontend/src/storybook/contents/Accordion.stories.tsx` 신규 작성. 사이드바 계층은 `Primitives/*`다(파일이 `contents/`에 있는 것과 무관하다 — `Card`/`Badge` 스토리도 같은 구조).

```tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite'

import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/primitives/accordion'

const meta = {
  title: 'Primitives/Accordion',
  component: Accordion,
  parameters: {
    docs: {
      description: {
        component:
          '펼침/접힘 블록. `type="single"` + `collapsible`이 기본 조합이고, `defaultValue`를 안 넘기면 접힌 상태로 시작한다. 열림 애니메이션은 없다.',
      },
    },
  },
} satisfies Meta<typeof Accordion>

export default meta
type Story = StoryObj<typeof meta>

/** 기본 — 접힌 상태로 시작한다. 헤더를 누르면 열리고, 다시 누르면 닫힌다(`collapsible`). */
export const Collapsed: Story = {
  render: () => (
    <Accordion type="single" collapsible className="max-w-80">
      <AccordionItem value="how-to-play">
        <AccordionTrigger>플레이 방법</AccordionTrigger>
        <AccordionContent>
          스코어를 정확히 맞히면 3점, 승·무·패만 맞혀도 2점을 받는다.
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  ),
}

/** `defaultValue`로 처음부터 펼친 상태. 값은 `AccordionItem`의 `value`와 같아야 한다. */
export const Expanded: Story = {
  render: () => (
    <Accordion type="single" collapsible defaultValue="how-to-play" className="max-w-80">
      <AccordionItem value="how-to-play">
        <AccordionTrigger>플레이 방법</AccordionTrigger>
        <AccordionContent>
          스코어를 정확히 맞히면 3점, 승·무·패만 맞혀도 2점을 받는다.
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  ),
}
```

- [ ] **Step 7: 스토리북이 뜨는지 확인**

```bash
cd frontend && npm run build-storybook
```
Expected: 빌드 성공(에러 없이 종료). 오래 걸리면 `npm run storybook`으로 띄워 `Primitives/Accordion`이 보이는지만 확인해도 된다.

- [ ] **Step 8: 커밋**

```bash
git add frontend/package.json frontend/package-lock.json frontend/src/components/primitives/accordion.tsx frontend/src/storybook/contents/Accordion.stories.tsx frontend/src/components/design-foundation.test.mjs
git commit -m "아코디언 프리미티브 신설 — Radix accordion 기반, foundation 검사 목록 등록"
```

---

### Task 3: `TeamBadge` 흑백 지원

**Files:**
- Modify: `frontend/src/components/composition/predict/shared.tsx:7-35`
- Modify: `frontend/src/storybook/contents/TeamBadge.stories.tsx`

- [ ] **Step 1: `grayscale` prop을 추가한다**

`frontend/src/components/composition/predict/shared.tsx`의 `TeamBadge`를 아래로 교체한다. `cn` import를 파일 상단에 추가해야 한다(지금은 없다).

```tsx
import { cn } from '@/lib/utils'
```

```tsx
/**
 * 팀 엠블럼(FotMob CDN URL은 lib/predictions/week.ts의 teamLogoUrl이 만든다). 실패하면 이니셜 원형으로 폴백.
 * `grayscale`은 "이 경기는 끝났다"는 표시다 — 판정은 경기 단위(`match.finished`)라, 한 주차 안에
 * 끝난 경기와 안 끝난 경기가 섞이면 로고 톤도 경기마다 갈린다.
 */
export function TeamBadge({
  logoUrl,
  name,
  size = 48,
  grayscale = false,
}: {
  logoUrl?: string
  name: string
  size?: number
  grayscale?: boolean
}) {
  const [failed, setFailed] = useState(false)

  if (failed || !logoUrl) {
    return (
      <span
        aria-hidden
        className={cn(
          'flex shrink-0 items-center justify-center rounded-pill bg-disabled text-label-1-normal font-black text-neutral-muted',
          grayscale && 'grayscale'
        )}
        style={{ width: size, height: size }}
      >
        {name.slice(0, 1)}
      </span>
    )
  }

  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src={logoUrl}
      alt=""
      width={size}
      height={size}
      onError={() => setFailed(true)}
      className={cn('shrink-0 object-contain', grayscale && 'grayscale')}
      style={{ width: size, height: size }}
    />
  )
}
```

- [ ] **Step 2: 타입 검사 통과 확인**

```bash
cd frontend && npx tsc --noEmit
```
Expected: 에러 없음

- [ ] **Step 3: 스토리에 흑백 케이스를 추가한다**

`frontend/src/storybook/contents/TeamBadge.stories.tsx`의 `argTypes`(17-28번째 줄)에 항목을 하나 더한다.

```tsx
    grayscale: {
      description: '종료된 경기(match.finished)의 로고를 흑백으로 가라앉힌다. 폴백 이니셜 원형에도 같이 적용된다.',
    },
```

그리고 파일 맨 아래에 스토리를 추가한다. `name`/`logoUrl`은 `meta.args`(29-32번째 줄)에서 상속된다.

```tsx
/**
 * 종료된 경기의 로고 — `grayscale`로 채도를 없애 "끝난 경기"를 표시한다. 판정 단위는 경기(`match.finished`)라,
 * 한 주차에 끝난 경기와 안 끝난 경기가 섞이면 로고 톤도 경기마다 갈린다.
 */
export const Grayscale: Story = {
  args: { grayscale: true },
}

/** 폴백 이니셜 원형에도 같은 규칙이 걸린다 — 로고 로드에 실패한 종료 경기. */
export const GrayscaleFallback: Story = {
  args: { grayscale: true, logoUrl: undefined },
}
```

- [ ] **Step 4: 전체 테스트 확인**

```bash
cd frontend && npm test
```
Expected: 전부 PASS (`shared.tsx`가 `design-foundation.test.mjs`의 `PREDICT_FILES`에 들어 있으므로 임의값 검사도 함께 돈다)

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/components/composition/predict/shared.tsx frontend/src/storybook/contents/TeamBadge.stories.tsx
git commit -m "TeamBadge에 grayscale prop 추가 — 종료된 경기 로고 흑백 처리용"
```

---

### Task 4: `MatchWeekList` 개편

의존: Task 3(`TeamBadge grayscale`), Task 1(`kickoff` 포맷 — 표시만 바뀌므로 코드 의존은 없다)

**Files:**
- Modify: `frontend/src/components/composition/predict/MatchWeekList.tsx` (93번째 줄 이후 전면 교체, 타입 정의 1-56은 유지)

- [ ] **Step 1: 상태 파생 함수 3개를 쓴다**

`MatchWeekList.tsx`의 기존 `matchStatusMeta`(81-91번째 줄)를 아래 3개 함수로 교체한다. import도 함께 정리한다 — `Lock`과 `badgeVariants`는 더 안 쓰고, `Badge`·`Button`이 새로 필요하다.

```tsx
import { ChevronLeft, ChevronRight } from 'lucide-react'
import { Badge } from '@/components/primitives/badge'
import { Button } from '@/components/primitives/button'
import { TeamBadge } from './shared'
import { cn } from '@/lib/utils'
```

```tsx
/**
 * 화면이 구분하는 주차 단계. `weekStatus`의 `'upcoming'`이 "아직 안 열림"과 "닫혔는데 결과가
 * 아직 안 적재됨" 두 뜻을 겸하고 있어서(lib/predictions/week.ts:35), 경기별 locked/finished로
 * 후자를 `'settling'`으로 갈라낸다. 순수 함수(weekStatus) 쪽은 건드리지 않는다.
 */
type WeekPhase = 'upcoming' | 'open' | 'settling' | 'result'

function weekPhase(week: PredictWeek): WeekPhase {
  if (week.status !== 'upcoming') return week.status
  return week.matches.some(match => match.locked && !match.finished) ? 'settling' : 'upcoming'
}

type BadgeVariant = 'default' | 'secondary' | 'outline'

/**
 * 배지는 **주차 단위**다 — 경기마다 붙이지 않는다(2026-08-25 확정).
 * 종료 주차는 참여했든 안 했든 "종료"이고, 참여 여부는 경기 카드 아래 내 점수 유무로 드러난다.
 */
function weekBadge(week: PredictWeek): { label: string; variant: BadgeVariant } {
  switch (weekPhase(week)) {
    case 'open':
      return week.hasPending
        ? { label: '진행중', variant: 'default' }
        : { label: '참여 완료', variant: 'secondary' }
    case 'settling':
      return { label: '결과 반영중', variant: 'outline' }
    case 'result':
      return { label: '종료', variant: 'outline' }
    default:
      return { label: '예정', variant: 'outline' }
  }
}

/**
 * 컨테이너 하단 CTA — 이 주차를 누르면 어디로 가는지 그대로 적는다. 컨테이너 자체는 클릭
 * 대상이 아니라(MatchdayHero와 같은 구조) 이 버튼만 진입점이다.
 * `settling`은 제출한 사람만 열어준다 — 미참여자는 그 화면에 보여줄 게 없다.
 */
function weekAction(week: PredictWeek): {
  label: string
  variant: 'default' | 'outline'
  disabled: boolean
} {
  switch (weekPhase(week)) {
    case 'open':
      // 부분 제출(일부 경기만 제출)이어도 문구는 같다 — 남은 경기를 예측하러 같은 자리로 들어간다.
      return week.hasPending
        ? { label: '예측하기', variant: 'default', disabled: false }
        : { label: '내 예측 보기', variant: 'outline', disabled: false }
    case 'settling':
      return week.submitted
        ? { label: '내 예측 보기', variant: 'outline', disabled: false }
        : { label: '예측 마감', variant: 'outline', disabled: true }
    case 'result':
      return { label: '결과보기', variant: 'outline', disabled: false }
    default:
      return { label: '예측 오픈 전', variant: 'outline', disabled: true }
  }
}

/**
 * 경기 카드 표면 — 예측할 수 있는(안 잠긴) 경기만 흰 표면을 쓴다. 잠긴 경기(종료·진행중)와
 * 아직 안 열린 주차의 경기는 가라앉혀서, 흰 카드가 "지금 할 수 있는 것"만 가리키게 한다.
 */
function isDimmed(week: PredictWeek, match: PredictWeekMatch): boolean {
  return match.locked || weekPhase(week) === 'upcoming'
}
```

- [ ] **Step 2: 리스트 본체를 쓴다**

기존 `MatchWeekList` 함수(93-155번째 줄)의 `<div className="flex flex-col gap-6">` 블록을 아래로 교체한다. 월 네비게이션 헤더(104-125번째 줄)는 그대로 둔다.

```tsx
      <div className="flex flex-col gap-4">
        {weeks.map((week, i) => (
          <WeekSessionCard
            key={week.weekKey}
            week={week}
            homeTeamName={homeTeamName}
            homeTeamLogoUrl={homeTeamLogoUrl}
            delayMs={i * 55}
            onSelect={onSelectWeek}
          />
        ))}
      </div>
```

- [ ] **Step 3: 주차 컨테이너를 쓴다**

기존 `WeekSessionCard`(161-219번째 줄)와 `StatusBadge`(221-224번째 줄), `WeekAction`(231-243번째 줄)을 아래 하나로 교체한다.

```tsx
/**
 * 주차 하나 = 컨테이너 하나 = 예측 세션 하나. 컨테이너는 `div`다(클릭 대상이 아니다) —
 * 안에 여러 경기 카드가 나란히 놓이고, 진입은 하단 버튼 하나로만 한다.
 * 경기가 1개든 2개든 같은 카드가 폭을 균등 분배한다(더블 매치위크를 따로 취급하지 않는다).
 */
function WeekSessionCard({
  week,
  homeTeamName,
  homeTeamLogoUrl,
  delayMs,
  onSelect,
}: {
  week: PredictWeek
  homeTeamName: string
  homeTeamLogoUrl?: string
  delayMs: number
  onSelect?: (week: PredictWeek) => void
}) {
  const badge = weekBadge(week)
  const action = weekAction(week)
  const isEmpty = week.matches.length === 0

  return (
    <section
      style={{ animationDelay: `${delayMs}ms` }}
      className="animate-enter rounded-lg border border-neutral-weak bg-surface p-4"
    >
      <div className="mb-3 flex items-center gap-2">
        <p className="text-label-1-normal font-extrabold text-neutral">{week.weekNo}주차</p>
        {!isEmpty && <Badge variant={badge.variant}>{badge.label}</Badge>}
      </div>

      {isEmpty ? (
        <p className="rounded-lg bg-disabled p-5 text-center text-caption-1 text-neutral-muted">
          이번 주는 예정된 경기가 없어요
        </p>
      ) : (
        <>
          <div className="flex flex-col gap-2 sm:flex-row">
            {week.matches.map(match => (
              <MatchInfoCard
                key={match.id}
                match={match}
                dimmed={isDimmed(week, match)}
                homeTeamName={homeTeamName}
                homeTeamLogoUrl={homeTeamLogoUrl}
              />
            ))}
          </div>

          <div className="mt-3 flex justify-end">
            <Button
              size="sm"
              variant={action.variant}
              disabled={action.disabled}
              onClick={action.disabled ? undefined : () => onSelect?.(week)}
            >
              {action.label}
            </Button>
          </div>
        </>
      )}
    </section>
  )
}
```

- [ ] **Step 4: 경기 카드를 쓴다**

기존 `MatchInfoRow`(245-302번째 줄)와 `TeamSide`(304-311번째 줄)를 아래로 교체한다.

```tsx
/**
 * 경기 하나 = 정보 카드 하나(클릭 대상이 아니다). 좌상단은 대회명만 둔다 — 이전에 있던
 * "N라운드"는 실제 라운드가 아니라 ISO 주차 번호였고(lib/predictions/week.ts:88-94),
 * `fixtures`에 라운드 컬럼이 없어서 삭제했다.
 * 데스크탑은 `flex-1`로 폭을 균등 분배하고 모바일은 세로로 쌓인다(MatchdayHero 평점 줄과 같다).
 */
function MatchInfoCard({
  match,
  dimmed,
  homeTeamName,
  homeTeamLogoUrl,
}: {
  match: PredictWeekMatch
  dimmed: boolean
  homeTeamName: string
  homeTeamLogoUrl?: string
}) {
  // 좌측 = 홈, 우측 = 원정 — isHome이 false면(원정 경기) 우리 팀이 우측으로 간다.
  const us = { name: homeTeamName, logoUrl: homeTeamLogoUrl }
  const them = { name: match.opponent, logoUrl: match.opponentLogoUrl }
  const [leftSide, rightSide] = match.isHome ? [us, them] : [them, us]
  const hasScore = typeof match.myResult?.totalPoints === 'number'

  return (
    <div
      className={cn(
        'flex-1 rounded-lg p-3',
        dimmed ? 'bg-disabled' : 'border border-neutral-weak bg-surface'
      )}
    >
      <p className={cn('mb-3 text-caption-1 font-bold', dimmed ? 'text-neutral-subtle' : 'text-neutral-muted')}>
        {match.competition ?? '프리미어리그'}
      </p>

      {/* 팀명 길이가 달라도 가운데(일자·시각)가 정중앙에 오도록 좌우를 동일 폭 1fr로 고정한다. */}
      <div className="grid grid-cols-[1fr_auto_1fr] items-center gap-2">
        <TeamSide
          name={leftSide.name}
          logoUrl={leftSide.logoUrl}
          grayscale={match.finished}
          dimmed={dimmed}
          align="end"
        />

        <div className="flex flex-col items-center gap-0.5">
          {/* 일자는 시각 위 caption — 월/일만 쓴다(연도 없음). */}
          <span className="text-caption-2 text-neutral-subtle">{match.kickoff}</span>
          {match.finished ? (
            <span className={cn('text-heading-1 font-black', dimmed ? 'text-neutral-muted' : 'text-neutral')}>
              {match.actual?.[0]} : {match.actual?.[1]}
            </span>
          ) : (
            <span className={cn('text-label-1-normal font-extrabold', dimmed ? 'text-neutral-muted' : 'text-neutral')}>
              {match.kickoffTime}
            </span>
          )}
        </div>

        <TeamSide
          name={rightSide.name}
          logoUrl={rightSide.logoUrl}
          grayscale={match.finished}
          dimmed={dimmed}
          align="start"
        />
      </div>

      {match.myResult && (
        <p className="mt-3 border-t border-neutral-weak pt-2 text-center text-caption-1 text-neutral-muted">
          예측 {match.myResult.predicted[0]}-{match.myResult.predicted[1]}
          {hasScore && <span className="font-extrabold text-brand"> +{match.myResult.totalPoints}점</span>}
        </p>
      )}
    </div>
  )
}

function TeamSide({
  name,
  logoUrl,
  grayscale,
  dimmed,
  align,
}: {
  name: string
  logoUrl?: string
  grayscale: boolean
  dimmed: boolean
  align: 'start' | 'end'
}) {
  return (
    <div
      className={cn(
        'flex w-20 flex-col items-center gap-1.5',
        align === 'end' ? 'justify-self-end' : 'justify-self-start'
      )}
    >
      <TeamBadge logoUrl={logoUrl} name={name} size={32} grayscale={grayscale} />
      <span
        className={cn(
          'w-full truncate text-center text-caption-1 font-bold',
          dimmed ? 'text-neutral-muted' : 'text-neutral'
        )}
      >
        {name}
      </span>
    </div>
  )
}
```

- [ ] **Step 5: 타입·테스트 확인**

```bash
cd frontend && npx tsc --noEmit && npm test
```
Expected: 타입 에러 없음 + 전부 PASS. `design-foundation.test.mjs`의 임의값 검사가 이 파일에 걸려 있으므로 여기서 걸리면 `text-[...]`/`bg-[#...]` 같은 게 남은 것이다.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/components/composition/predict/MatchWeekList.tsx
git commit -m "승부예측 목록 주차 컨테이너 개편 — 경기 카드 균등 분배, 배지 5종·CTA 버튼, 종료 경기 흑백, 라운드 표기 삭제"
```

---

### Task 5: `Explanation` 컴포넌트 신설

의존: Task 2(`accordion` 프리미티브)

**Files:**
- Create: `frontend/src/components/composition/common/Explanation.tsx`

- [ ] **Step 1: 컴포넌트를 쓴다**

`frontend/src/components/composition/common/Explanation.tsx` 신규 작성.

```tsx
'use client'

import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/primitives/accordion'

/**
 * 접힌 상태로 시작하는 설명 블록 — 화면의 주 내용이 아니라 "필요하면 펼쳐 보는" 보조 정보다.
 * 어느 화면에서든 쓸 수 있게 문구를 갖고 있지 않다: 제목과 항목을 호출부가 넘긴다.
 */
export function Explanation({
  title,
  items,
  className,
}: {
  title: string
  /** 순서가 의미 있는 설명 항목들 — 번호 매긴 목록으로 렌더된다 */
  items: string[]
  className?: string
}) {
  return (
    <Accordion type="single" collapsible className={className}>
      <AccordionItem value="explanation">
        <AccordionTrigger>{title}</AccordionTrigger>
        <AccordionContent>
          <ol className="list-decimal space-y-1.5 pl-4">
            {items.map(item => (
              <li key={item}>{item}</li>
            ))}
          </ol>
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  )
}
```

- [ ] **Step 2: 타입 검사**

```bash
cd frontend && npx tsc --noEmit
```
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/components/composition/common/Explanation.tsx
git commit -m "Explanation 컴포넌트 신설 — 접힌 아코디언으로 보조 설명을 보여주는 범용 블록"
```

---

### Task 6: `MatchWeekList` 스토리 재작성

의존: Task 4

**Files:**
- Modify: `frontend/src/storybook/contents/MatchWeekList.stories.tsx`

- [ ] **Step 1: mock 헬퍼의 일자 포맷을 새 형식으로 바꾼다**

`mockMatch`의 기본값(18번째 줄)과 각 스토리의 `kickoff` 인자를 `'8월 2일'` 형태로 바꾼다.

```tsx
    kickoff: '8월 2일',
```

- [ ] **Step 2: 상태 스토리를 7개로 다시 쓴다**

기존 `Open` / `Submitted` / `Result` / `Upcoming` / `DoubleMatchWeek` / `PartiallySubmitted`를 아래로 교체한다. 배지·CTA가 주차 단계에서 파생되므로 스토리도 그 5단계를 그대로 덮는다.

```tsx
/** 진행중 — 제출할 경기가 남은 주차. 배지 `진행중` + 솔리드 "예측하기" 버튼. */
export const Open: Story = {
  args: {
    weeks: [mockWeek({ weekNo: 33, status: 'open', matches: [mockMatch({ id: 'w33', opponent: '아스날', isHome: false, kickoff: '7월 19일' })] })],
  },
}

/** 참여 완료 — 제출 가능한 경기를 다 제출한 주차. 경기는 아직 안 끝났고 CTA는 "내 예측 보기"다. */
export const Submitted: Story = {
  args: {
    weeks: [
      mockWeek({
        weekNo: 33,
        status: 'open',
        matches: [mockMatch({ id: 'w33s', opponent: '아스날', kickoff: '7월 19일', myResult: { predicted: [2, 1] } })],
      }),
    ],
  },
}

/**
 * 부분 제출 — 첫 경기는 킥오프가 지나 잠기고(`locked`) 둘째 경기만 남은 상태.
 * `submitted`이면서 동시에 `hasPending`이라 배지는 `진행중`, CTA는 미제출 주차와 같은 "예측하기"다.
 * 잠긴 첫 경기 카드만 회색으로 가라앉는다.
 */
export const PartiallySubmitted: Story = {
  args: {
    weeks: [
      mockWeek({
        weekNo: 40,
        status: 'open',
        matches: [
          mockMatch({ id: 'w40a', competition: '프리미어리그', opponent: '첼시', kickoff: '8월 30일', locked: true, myResult: { predicted: [1, 2] } }),
          mockMatch({ id: 'w40b', competition: '카라바오컵', opponent: '브라이튼', isHome: false, kickoff: '9월 2일' }),
        ],
      }),
    ],
  },
}

/**
 * 결과 반영중 — 킥오프는 지났는데 `finished`가 아직 안 적재된 주차(`status: 'upcoming'`인데
 * 경기가 `locked`). 참여자는 "내 예측 보기"로 들어갈 수 있고, 미참여자는 "예측 마감"으로 막힌다.
 */
export const Settling: Story = {
  args: {
    weeks: [
      mockWeek({
        weekNo: 34,
        status: 'upcoming',
        matches: [mockMatch({ id: 'w34p', opponent: '리버풀', kickoff: '8월 24일', locked: true, myResult: { predicted: [2, 1] } })],
      }),
      mockWeek({
        weekNo: 34,
        weekKey: '2026-34-none',
        status: 'upcoming',
        matches: [mockMatch({ id: 'w34n', opponent: '리버풀', kickoff: '8월 24일', locked: true })],
      }),
    ],
  },
}

/**
 * 종료 — 그 주 경기가 다 끝난 주차. 배지는 참여 여부와 무관하게 `종료` 하나이고,
 * 참여 여부는 카드 아래 "예측 2-1 · +7점" 유무로만 드러난다. 로고는 흑백이 된다.
 */
export const Result: Story = {
  args: {
    weeks: [
      mockWeek({
        weekNo: 30,
        status: 'result',
        matches: [finishedMatch({ id: 'w30', opponent: '맨체스터시티', isHome: false, kickoff: '6월 28일', kickoffTime: '오후 9:00', actual: [2, 1] })],
      }),
      mockWeek({
        weekNo: 32,
        status: 'result',
        matches: [finishedMatch({ id: 'w32', opponent: '리버풀', kickoff: '7월 12일', actual: [2, 0], myResult: { predicted: [2, 1], totalPoints: 7 } })],
      }),
    ],
  },
}

/** 예정 — 아직 안 열린 주차. 배지 `예정` + disabled "예측 오픈 전" 버튼, 경기 카드도 가라앉는다. */
export const Upcoming: Story = {
  args: {
    weeks: [mockWeek({ weekNo: 34, status: 'upcoming', matches: [mockMatch({ id: 'w34', opponent: '토트넘', kickoff: '7월 26일', kickoffTime: '오후 9:00' })] })],
  },
}

/**
 * 경기 2개인 주차 — 카드 두 장이 폭을 반씩 나눠 갖는다(모바일에서는 세로로 쌓인다).
 * 주차 단위로 배지 하나, CTA 하나다 — 경기마다 배지를 붙이지 않는다.
 */
export const TwoMatches: Story = {
  args: {
    weeks: [
      mockWeek({
        weekNo: 39,
        status: 'open',
        matches: [
          mockMatch({ id: 'w39a', competition: '프리미어리그', opponent: '에버튼', kickoff: '8월 23일' }),
          mockMatch({ id: 'w39b', competition: '카라바오컵', opponent: '브렌트포드', isHome: false, kickoff: '8월 26일' }),
        ],
      }),
    ],
  },
}
```

- [ ] **Step 3: 경기 없는 주 스토리를 고친다**

`NoMatches`의 주석을 아래로 바꾼다(구조가 달라졌다).

```tsx
/** 경기 없는 주 — 컨테이너와 "N주차" 타이틀은 다른 주차와 같고, 경기 카드 자리에 회색 안내 박스가 들어간다. 배지·버튼은 없다. */
```

- [ ] **Step 4: 데스크탑 폭 스토리를 추가한다**

파일의 `mobileWidth` 데코레이터는 358px로 고정돼 있어 가로 배치가 안 보인다. 맨 아래에 데스크탑 폭 스토리를 추가한다.

```tsx
/**
 * 데스크탑 폭 — 경기 카드가 가로로 균등 분배되는 걸 확인하는 스토리.
 * 다른 스토리들은 모바일 폭(358px)에 고정돼 있어 세로 배열만 보인다.
 */
export const DesktopWidth: Story = {
  decorators: [(Story: () => React.JSX.Element) => <div style={{ maxWidth: 720 }}><Story /></div>],
  args: {
    weeks: [
      mockWeek({
        weekNo: 39,
        status: 'open',
        matches: [
          mockMatch({ id: 'd39a', competition: '프리미어리그', opponent: '에버튼', kickoff: '8월 23일' }),
          mockMatch({ id: 'd39b', competition: '카라바오컵', opponent: '브렌트포드', isHome: false, kickoff: '8월 26일' }),
        ],
      }),
      mockWeek({
        weekNo: 38,
        status: 'result',
        matches: [finishedMatch({ id: 'd38', opponent: '풀럼', isHome: false, kickoff: '8월 16일', actual: [2, 1], myResult: { predicted: [2, 1], totalPoints: 10 } })],
      }),
    ],
  },
}
```

- [ ] **Step 5: 남은 스토리의 일자를 새 형식으로 맞춘다**

`HomeAndAway`(2건: `7월 12일`, `7월 19일`)와 `FullMonth`(6건: `8월 2일`, `8월 5일`, `8월 9일`, `8월 16일`, `8월 23일`, `8월 30일`)의 `kickoff` 값을 바꾼다. 파일 전체에서 `kickoff: '숫자/숫자'` 패턴이 남아 있지 않은지 확인한다.

```bash
cd frontend && grep -n "kickoff: '[0-9]*/[0-9]*'" src/storybook/contents/MatchWeekList.stories.tsx
```
Expected: 출력 없음

- [ ] **Step 6: 스토리북 빌드 확인**

```bash
cd frontend && npm run build-storybook
```
Expected: 빌드 성공

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/storybook/contents/MatchWeekList.stories.tsx
git commit -m "MatchWeekList 스토리를 주차 단계 5종 기준으로 재작성 — 데스크탑 폭 스토리 추가"
```

---

### Task 7: `PredictListClient` — 정렬과 플레이 방법 배치

의존: Task 1(`openWeeksFirst`), Task 5(`Explanation`)

**Files:**
- Modify: `frontend/src/components/composition/predict/PredictListClient.tsx`

- [ ] **Step 1: 플레이 방법 문구와 import를 추가한다**

파일 상단 import에 두 줄을 더한다.

```tsx
import { Explanation } from '@/components/composition/common/Explanation'
```

`toPredictWeeks` import 뒤에 `openWeeksFirst`를 추가한다.

```tsx
import {
  NUFC_LABEL,
  NUFC_TEAM_ID,
  openWeeksFirst,
  teamLogoUrl,
  toPredictWeeks,
  type WeekGroup,
} from '@/lib/predictions/week'
```

컴포넌트 **밖**(import 아래)에 문구를 상수로 둔다 — 데스크탑 우측 열과 모바일 목록 하단 두 곳에서 같은 값을 쓴다.

```tsx
/**
 * 플레이 방법 — 요구사항 명세서에서 **확정**된 항목만 적는다.
 * 배당 가중치(FR-005)는 미확정이라 넣지 않는다.
 */
const PLAY_GUIDE = [
  '스코어를 정확히 맞히면 3점, 승·무·패만 맞혀도 2점',
  '포지션마다 활약할 선수 1명 — 평점 7.0 이상이면 점수, 미출전은 0점',
  '첫 경기 7일 전 오픈 · 마지막 경기 킥오프에 마감 · 제출 후 수정 불가',
]
```

- [ ] **Step 2: 정렬을 적용한다**

36번째 줄의 `visibleWeeks`를 바꾼다.

```tsx
  // 활성 주차를 그 달 맨 위로 — 나머지는 킥오프 오름차순 그대로다(lib/predictions/week.ts).
  const visibleWeeks = openWeeksFirst(weeks.filter(w => w.monthKey === monthKey))
```

- [ ] **Step 3: 플레이 방법을 두 자리에 넣는다**

우측 열(72-75번째 줄)에 세 번째 카드로 추가하고, 2단 그리드 **뒤에** 모바일 전용 블록을 하나 더 둔다.

```tsx
        <div className="hidden flex-col gap-4 sm:flex">
          <RankingCard variant="top3" entries={ranking} />
          <RankingCard variant="mine" entries={ranking} />
          <Explanation title="플레이 방법" items={PLAY_GUIDE} />
        </div>
      </div>

      {/* 우측 열이 모바일에서 숨겨지므로(위 sm:flex) 플레이 방법만 목록 맨 아래에 한 번 더 둔다. */}
      <Explanation title="플레이 방법" items={PLAY_GUIDE} className="mt-4 sm:hidden" />
```

- [ ] **Step 4: 타입·테스트 확인**

```bash
cd frontend && npx tsc --noEmit && npm test
```
Expected: 전부 PASS. 특히 `lib/analytics/analytics-contract.test.mjs`가 이 파일에 CTA 문구(`예측하기|결과보기|제출완료`)가 없어야 한다고 단정한다 — `PLAY_GUIDE` 문구에는 그 단어가 없으므로 통과한다.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/components/composition/predict/PredictListClient.tsx
git commit -m "승부예측 목록에 활성 주차 우선 정렬 적용, 플레이 방법을 우측 열 하단·모바일 목록 하단에 배치"
```

---

### Task 8: 전체 검증

**Files:** 없음(검증만)

- [ ] **Step 1: 전체 테스트**

```bash
cd frontend && npm test
```
Expected: 전부 PASS

- [ ] **Step 2: lint**

```bash
cd frontend && npm run lint
```
Expected: 에러 없음

- [ ] **Step 3: 빌드**

```bash
cd frontend && npm run build
```
Expected: 성공

- [ ] **Step 4: mock 모드로 화면 확인**

```bash
cd frontend && npm run dev
```
`http://localhost:3000/predictions`에서 확인할 것:
- 활성 주차가 그 달 맨 위에 있는지
- 데스크탑에서 경기 2개 주차의 카드가 반씩 나뉘는지, 모바일 폭에서 세로로 쌓이는지
- 종료된 경기의 로고가 흑백인지
- 잠긴 주차의 버튼이 disabled("예측 오픈 전")인지
- 우측 열 맨 아래와 모바일 목록 맨 아래에 접힌 "플레이 방법"이 있는지
- 일자가 "8월 27일" 형식인지

mock 데이터(`lib/mock/data.ts:334-344`)는 오늘 기준 상대 날짜라 종료/진행중/예정이 항상 다 나온다. 단 `결과 반영중`(킥오프 지남 · 미종료) 상태는 mock에 없다 — 그 상태는 Storybook `Settling` 스토리로 확인한다.
